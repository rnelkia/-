# フォルダ自動整理ツール

## 概要
指定したフォルダ内のファイルを拡張子ごとに自動で整理するPythonツールです。

## 機能
- ファイルを種類ごとに分類（画像、PDF、Excelなど）
- フォルダがない場合は自動生成
- GUIでフォルダ選択可能

## 使用技術
- Python
- os / shutil
- tkinter

## 工夫した点
ユーザーが簡単に操作できるよう、フォルダ選択をGUIで実装しました。
また、拡張子ごとに分類ルールを定義することで柔軟に対応できる設計にしました。
